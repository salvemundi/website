'use client';



// The mobile navigation is currently handled by the Header component (hamburger menu).
// This component is kept as a placeholder if a separate bottom navigation bar is needed in the future.
export default function BottomNav() {
    return null;
}
