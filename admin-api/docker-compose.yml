services:
  admin-api:
    image: ghcr.io/salvemundi/admin-api:${TAG:-dev}
    container_name: admin-api-${TAG:-dev}
    restart: unless-stopped
    env_file: .env
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
