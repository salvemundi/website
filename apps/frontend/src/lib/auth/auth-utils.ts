/**
 * Authentication and authorization utilities for Salve Mundi V7.
 */

import { type Committee } from '@/shared/lib/permissions';

// Re-export Committee so existing imports of it from '@/lib/auth' keep working
export type { Committee };
