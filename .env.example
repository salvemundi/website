# Directus Configuration
VITE_DIRECTUS_URL=https://admin.salvemundi.nl
VITE_DIRECTUS_API_KEY=your_api_key_here

# Microsoft Entra ID (Azure AD) Configuration
VITE_ENTRA_CLIENT_ID=your_client_id_here
VITE_ENTRA_TENANT_ID=your_tenant_id_here
VITE_ENTRA_REDIRECT_URI=http://localhost:5173

# Default User Role (optional)
VITE_DEFAULT_USER_ROLE_ID=

# Email Notification Configuration
# Email API endpoint for sending notification emails
# Leave empty to disable email notifications (functionality will still work)
# 
# ⚠️ WARNING: Microsoft Graph API cannot be used directly from frontend!
# Microsoft Graph requires server-side authentication with access tokens.
# 
# Options:
# 1. Use Directus email endpoint (recommended if using Directus)
#    Example: VITE_EMAIL_API_ENDPOINT=https://admin.salvemundi.nl/flows/trigger/send-email
# 
# 2. Create a backend API endpoint that handles email sending
#    Example: VITE_EMAIL_API_ENDPOINT=https://api.salvemundi.nl/send-email
# 
# 3. Use a serverless function (Vercel, Netlify, etc.)
#    Example: VITE_EMAIL_API_ENDPOINT=/api/send-email
#
# See readme/EMAIL_SETUP.md for detailed setup instructions
VITE_EMAIL_API_ENDPOINT=

# Email sender configuration (optional - defaults will be used if not set)
VITE_EMAIL_FROM=noreply@salvemundi.nl
VITE_EMAIL_FROM_NAME=Salve Mundi
